# AI Service Requirements
# For GPU server - YOLO training, inference, and model conversion

# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Deep Learning - PyTorch 2.6+ with CUDA 12.6
# Using latest stable versions
torch>=2.6.0
torchvision>=0.21.0

# YOLO - Latest version (supports PyTorch 2.6+)
ultralytics>=8.3.0

# Model conversion dependencies
onnx>=1.18.0              # ONNX format support
onnxruntime>=1.16.0       # ONNX runtime for validation
# tensorrt>=8.6.0         # TensorRT (install separately from NVIDIA)

# Utilities
httpx==0.25.1
python-multipart==0.0.6
Pillow==10.1.0
numpy==1.24.3
pyyaml==6.0.1
requests>=2.31.0          # For API examples

# Optional but recommended
gpustat==1.1  # GPU monitoring
