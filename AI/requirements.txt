# AI Multi-Service Requirements
# RabbitMQ-based YOLO training, conversion, and inference

# Deep Learning - PyTorch with CUDA support
torch>=2.6.0
torchvision>=0.21.0

# YOLO framework (training, inference, conversion)
ultralytics>=8.3.0

# Model conversion dependencies
onnx>=1.18.0              # ONNX format support
onnxruntime>=1.16.0       # ONNX runtime for validation
# tensorrt>=8.6.0         # TensorRT (install separately from NVIDIA)

# Message Queue
pika>=1.3.0               # RabbitMQ client

# Core utilities
Pillow>=10.1.0            # Image processing
numpy>=1.24.3             # Numerical computing
pyyaml>=6.0.1             # YAML configuration files

# Optional but recommended
gpustat>=1.1              # GPU monitoring
